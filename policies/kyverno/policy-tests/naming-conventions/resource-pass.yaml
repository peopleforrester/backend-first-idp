# ABOUTME: Test resource that should PASS naming convention — follows {team}-{resource} pattern.
# ABOUTME: Claim names must start with the team name.
apiVersion: platform.kubecon.io/v1alpha1
kind: DatabaseInstanceClaim
metadata:
  name: checkout-db
  namespace: checkout
  labels:
    team: checkout
spec:
  size: small
  region: eu-west-1
  team: checkout
