# ABOUTME: Test resource that should FAIL required-labels — missing team label.
# ABOUTME: All platform claims must have a team label in metadata.labels.
apiVersion: platform.kubecon.io/v1alpha1
kind: DatabaseInstanceClaim
metadata:
  name: checkout-db-no-label
  namespace: checkout
spec:
  size: small
  region: eu-west-1
  team: checkout
