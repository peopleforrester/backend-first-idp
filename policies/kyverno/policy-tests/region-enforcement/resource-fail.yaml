# ABOUTME: Test resource that should FAIL region enforcement — checkout in us-west-2.
# ABOUTME: checkout team is restricted to EU regions only (PCI-DSS).
apiVersion: platform.kubecon.io/v1alpha1
kind: DatabaseInstanceClaim
metadata:
  name: checkout-db-bad-region
  namespace: checkout
  labels:
    team: checkout
spec:
  size: small
  region: us-west-2
  team: checkout
