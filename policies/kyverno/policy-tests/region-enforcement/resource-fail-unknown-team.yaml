# ABOUTME: Test resource that should FAIL — unknown team not in onboarded list.
# ABOUTME: Default-deny rule rejects claims from teams not in the allowlist.
apiVersion: platform.kubecon.io/v1alpha1
kind: DatabaseInstanceClaim
metadata:
  name: rogue-team-db
  namespace: rogue-team
  labels:
    team: rogue-team
spec:
  size: small
  region: eu-west-1
  team: rogue-team
