# ABOUTME: Test resource that should PASS region enforcement — checkout in eu-west-1.
# ABOUTME: Used by kyverno CLI apply tests.
apiVersion: platform.kubecon.io/v1alpha1
kind: DatabaseInstanceClaim
metadata:
  name: checkout-db-test
  namespace: checkout
  labels:
    team: checkout
spec:
  size: small
  region: eu-west-1
  team: checkout
