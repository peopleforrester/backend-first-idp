# ABOUTME: CacheInstanceClaim for catalog team — catalog-search-cache-dev.
# ABOUTME: Generated from scripts/teams.yaml by generate-team-claims.py.
apiVersion: platform.kubecon.io/v1alpha1
kind: CacheInstanceClaim
metadata:
  name: catalog-search-cache-dev
  namespace: catalog
  labels:
    team: catalog
spec:
  team: catalog
  region: eu-west-1
  size: small
  engine: redis
