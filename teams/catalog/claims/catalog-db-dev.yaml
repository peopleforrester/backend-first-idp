# ABOUTME: DatabaseInstanceClaim for catalog team — catalog-db-dev.
# ABOUTME: Generated from scripts/teams.yaml by generate-team-claims.py.
apiVersion: platform.kubecon.io/v1alpha1
kind: DatabaseInstanceClaim
metadata:
  name: catalog-db-dev
  namespace: catalog
  labels:
    team: catalog
spec:
  team: catalog
  region: eu-west-1
  size: small
  engine: postgres
  backupRetentionDays: 7
