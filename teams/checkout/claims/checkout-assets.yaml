# ABOUTME: ObjectStorageClaim for checkout team — checkout-assets.
# ABOUTME: Generated from scripts/teams.yaml by generate-team-claims.py.
apiVersion: platform.kubecon.io/v1alpha1
kind: ObjectStorageClaim
metadata:
  name: checkout-assets
  namespace: checkout
  labels:
    team: checkout
spec:
  team: checkout
  region: eu-west-1
  versioning: false
  encryption: true
