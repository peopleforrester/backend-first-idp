# ABOUTME: DatabaseInstanceClaim for checkout team — checkout-db.
# ABOUTME: Generated from scripts/teams.yaml by generate-team-claims.py.
apiVersion: platform.kubecon.io/v1alpha1
kind: DatabaseInstanceClaim
metadata:
  name: checkout-db
  namespace: checkout
  labels:
    team: checkout
spec:
  team: checkout
  region: eu-west-1
  size: small
  engine: postgres
  backupRetentionDays: 7
