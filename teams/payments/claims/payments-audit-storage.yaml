# ABOUTME: ObjectStorageClaim for payments team — payments-audit-storage.
# ABOUTME: Generated from scripts/teams.yaml by generate-team-claims.py.
apiVersion: platform.kubecon.io/v1alpha1
kind: ObjectStorageClaim
metadata:
  name: payments-audit-storage
  namespace: payments
  labels:
    team: payments
spec:
  team: payments
  region: eu-central-1
  encryption: true
  versioning: true
