# ABOUTME: DatabaseInstanceClaim for platform team — platform-db-dev.
# ABOUTME: Generated from scripts/teams.yaml by generate-team-claims.py.
apiVersion: platform.kubecon.io/v1alpha1
kind: DatabaseInstanceClaim
metadata:
  name: platform-db-dev
  namespace: platform
  labels:
    team: platform
spec:
  team: platform
  region: eu-west-1
  size: small
  engine: postgres
  backupRetentionDays: 7
