# ABOUTME: DatabaseInstanceClaim for identity team — identity-auth-db-staging.
# ABOUTME: Generated from scripts/teams.yaml by generate-team-claims.py.
apiVersion: platform.kubecon.io/v1alpha1
kind: DatabaseInstanceClaim
metadata:
  name: identity-auth-db-staging
  namespace: identity
  labels:
    team: identity
spec:
  team: identity
  region: eu-west-1
  size: small
  engine: postgres
  backupRetentionDays: 3
