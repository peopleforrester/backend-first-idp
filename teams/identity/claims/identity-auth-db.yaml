# ABOUTME: DatabaseInstanceClaim for identity team — identity-auth-db.
# ABOUTME: Generated from scripts/teams.yaml by generate-team-claims.py.
apiVersion: platform.kubecon.io/v1alpha1
kind: DatabaseInstanceClaim
metadata:
  name: identity-auth-db
  namespace: identity
  labels:
    team: identity
spec:
  team: identity
  region: eu-west-1
  size: medium
  engine: postgres
  highAvailability: true
  backupRetentionDays: 14
