# ABOUTME: ObjectStorageClaim for identity team — identity-keys-storage.
# ABOUTME: Generated from scripts/teams.yaml by generate-team-claims.py.
apiVersion: platform.kubecon.io/v1alpha1
kind: ObjectStorageClaim
metadata:
  name: identity-keys-storage
  namespace: identity
  labels:
    team: identity
spec:
  team: identity
  region: eu-west-1
  versioning: true
  encryption: true
