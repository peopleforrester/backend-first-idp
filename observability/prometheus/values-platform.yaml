# ABOUTME: Helm values for kube-prometheus-stack tailored to the platform.
# ABOUTME: Enables Grafana with dashboard auto-provisioning and platform-specific retention.
prometheus:
  prometheusSpec:
    retention: 30d
    retentionSize: 50GB
    storageSpec:
      volumeClaimTemplate:
        spec:
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 100Gi
    serviceMonitorSelector:
      matchLabels:
        prometheus: platform
    ruleSelector:
      matchLabels:
        prometheus: platform

grafana:
  enabled: true
  # Admin credentials are pulled from the cluster's secret manager via ESO.
  # See secrets/eso/external-secrets/grafana-admin.yaml. The chart reads
  # admin-user and admin-password keys from this Secret.
  admin:
    existingSecret: grafana-admin
    userKey: admin-user
    passwordKey: admin-password
  sidecar:
    dashboards:
      enabled: true
      searchNamespace: observability
      label: grafana_dashboard
      folderAnnotation: grafana_folder
    datasources:
      enabled: true
  dashboardProviders:
    dashboardproviders.yaml:
      apiVersion: 1
      providers:
        - name: platform
          orgId: 1
          folder: Platform
          type: file
          disableDeletion: false
          editable: true
          options:
            path: /var/lib/grafana/dashboards/platform

alertmanager:
  enabled: true
  alertmanagerSpec:
    retention: 120h
