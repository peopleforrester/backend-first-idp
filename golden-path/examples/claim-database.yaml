# ABOUTME: Working DatabaseInstance claim — the entire golden path in 9 lines.
# ABOUTME: This is what developers actually interact with. Everything else is platform.
apiVersion: platform.kubecon.io/v1alpha1
kind: DatabaseInstanceClaim
metadata:
  name: checkout-db
  namespace: checkout
  labels:
    team: checkout
spec:
  size: small
  region: eu-west-1
  team: checkout
